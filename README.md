# JFileTransfer

- Can't send folders (but it can send compressed archives)
- Works only in LAN
- No GUI

## Usage:
Compile everything
```bash
javac *.java
```

Run App.java
```bash
java App
```

**Currently WIP**
